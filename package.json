{
  "devDependencies": {
    "vitepress": "^1.4.1",
    "vue": "^3.4.34"
  },
  "scripts": {
    "docs:dev": "DEBUG=true vitepress dev web",
    "docs:build": "./validate-json-files.sh && DEBUG=true vitepress build web",
    "docs:build:ci": "./validate-json-files.sh && npm run docs:build",
    "docs:preview": "DEBUG=true vitepress preview web"
  },
  "dependencies": {
    "rollup": "^4.24.0"
  }
}
